# Exemple : Namespaces sans autoloader

Cet exemple illustre :

- L'utilisation de namespaces pour organiser le code.
- N'utilise pas d'autoloader, donc les fichiers doivent être inclus manuellement
  avec `require_once`.
